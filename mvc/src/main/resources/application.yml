spring:
  application:
    name: MVC
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb #connection string
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
